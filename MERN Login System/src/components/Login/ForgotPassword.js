import React from 'react';
import { withRouter } from "react-router-dom"

const ForgotPassword = () => {
    return ( 
    <div><p>ForgotPassword</p></div>
    );
}

export default withRouter(ForgotPassword);